[tool.pytest.ini_options]
norecursedirs = [
    ".git", "__pycache__", "*.egg-info",
    ".hypothesis", ".pytest_cache", "data",
    "node_modules",
]

[tool.setuptools.packages.find]
include = ["agents*", "src*", "eval*"]
